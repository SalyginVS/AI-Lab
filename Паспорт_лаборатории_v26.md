# Паспорт лаборатории локального LLM-сервера (Lab RTX3090)

**Версия:** 26.0  
**Обновлён:** 2026-04-08  
**По результатам:** Этапов 1–16, 13  
**Целевая архитектура:** Целевая_архитектура_AI_Coding_Platform v1.12

> **Назначение документа.** Паспорт фиксирует **фактическое состояние** конкретного лабораторного стенда на дату версии. Является снимком реализации целевой архитектуры (source of truth — единого источника правды). Любое расхождение между паспортом и фактическим стеком считается техническим долгом. Любое расхождение между паспортом и целевой архитектурой — либо устраняется, либо оформляется явно как архитектурное исключение.

---

## 1. Назначение лаборатории

Цель: отладка и эксплуатация локального стек-паттерна для LLM (Large Language Model — большая языковая модель) без облаков — один Ubuntu-сервер с GPU (Graphics Processing Unit — графический процессор), Ollama как менеджер моделей, OpenAI-совместимый HTTP-шлюз, подключение IDE (Integrated Development Environment — интегрированная среда разработки: VSCode + Continue.dev + Copilot BYOK) и CLI-клиентов.

Стратегия использования — **Depth over Speed**: приоритет максимальной глубины контекста и работы с тяжёлыми reasoning-моделями, используя системную ОЗУ (RAM — Random Access Memory — оперативная память) как расширение видеопамяти. Скорость генерации (tokens/sec) — вторичный приоритет.

Задачи: тестирование больших локальных моделей (Qwen, DeepSeek, GLM, Gemma и др.) под реальной инженерной нагрузкой; разработка и проверка архитектурных паттернов (шлюзы, API — Application Programming Interface, мониторинг, интеграция с Dev-инструментами); подготовка решений, переносимых на прод-серверы.

---

## 2. Аппаратная конфигурация

**Сервер:** x86_64, ПК под Ubuntu Server 24.04. CPU (Central Processing Unit — центральный процессор): Intel Core i9-9900KF (8C/16T). RAM: 62 ГБ. SSD: 1 ТБ (159 ГБ занято, 731 ГБ свободно).

**GPU:** NVIDIA GeForce RTX 3090 Phantom GS. VRAM (Video RAM — видеопамять): 24 ГБ GDDR6X. Драйвер: 590.48.01, CUDA (Compute Unified Device Architecture) 13.1.

**Суммарная память (Depth over Speed):** 24 ГБ VRAM + 62 ГБ RAM = ~86 ГБ.

---

## 3. Базовое ПО и сеть

**ОС:** Ubuntu Server 24.04 (headless). **Сеть:** IP `192.168.0.128` (DHCP). SSH-доступ из локальной сети. **UFW (Uncomplicated Firewall):** Active (включён в этапе 11, Грабли #42).

---

## 4. GPU-стек

NVIDIA драйвер 590.48.01, CUDA 13.1. Docker Engine 28.2.2 + NVIDIA Container Toolkit.

---

## 5. LLM-уровень: Ollama и модели

### 5.1. Ollama — конфигурация

Адрес: `http://localhost:11434` / `http://192.168.0.128:11434`. Systemd: `ollama.service`. Версия: **0.20.2**.

Override `/etc/systemd/system/ollama.service.d/override.conf`:

| Переменная | Значение | Назначение |
|---|---|---|
| `OLLAMA_HOST` | `0.0.0.0:11434` | Все интерфейсы |
| `OLLAMA_FLASH_ATTENTION` | `1` | Flash Attention (оптимизация вычисления внимания) |
| `OLLAMA_KV_CACHE_TYPE` | `q8_0` | KV-кэш (Key-Value cache) квантизация до 8 бит |
| `OLLAMA_MAX_LOADED_MODELS` | `2` | FIM + chat/agent одновременно |
| `OLLAMA_NUM_PARALLEL` | `1` | Один параллельный запрос к модели (autocomplete и chat конкурируют за GPU) |
| `CUDA_VISIBLE_DEVICES` | `0` | GPU 0 |
| `GGML_CUDA_NO_GRAPHS` | `1` | Отключение CUDA graph capture |

**Политика квантизации и KV-кэша.** Лаборатория использует только схемы квантизации, официально поддержанные в Ollama/llama.cpp. Текущая схема KV-кэша: `q8_0` (официально поддержана с Ollama 0.4+). Исследовательские схемы допускаются только как изолированный PoC (Proof of Concept — проверка концепции) без влияния на основной контур. Переход на новую схему требует: (1) официальной поддержки в используемой версии Ollama, (2) A/B-сравнения, (3) записи результата в ADR (Architecture Decision Record — документ архитектурного решения).

### 5.2. Модели (15 штук)

| # | Модель | Disk | Роль | Статус | Примечания |
|---|--------|------|------|--------|-----------|
| 1 | **gemma4:31b** | 19 ГБ | **Quality-first Agent / Planner / Reviewer** | **Active** | #3 Arena open. arch=gemma4, 31.3B, ctx 262144, Q4_K_M. Capabilities: completion, vision, tools, thinking. Требует thin policy для tool selection (ADR-012). **Long-context (65K+) blocked:** upstream bug Ollama/llama.cpp SWA (Грабли #43). Стабилен при ctx ≤ 32768. |
| 2 | **gemma4:26b** | 17 ГБ | **Fast Semantic Agent / Fast Executor** | **Active** | #6 Arena open. arch=gemma4, MoE 25.8B, ctx 262144, Q4_K_M. Capabilities: completion, vision, tools, thinking. Лучший zero-shot semantic tool selector (10C). **Long-context (65K+) blocked:** upstream bug (Грабли #43). Стабилен при ctx ≤ 32768. |
| 3 | **gemma4:e4b** | 9.6 ГБ | Fast edge chat (evaluation) | **PoC** | PLE 4.5B effective, 128K ctx. Частично протестирована в 10C. |
| 4 | qwen3-coder:30b | 18 ГБ | **Stable Generic Executor / Backup Agent / Best SQL Executor** | **Active** | arch=qwen3moe (MoE), 30.5B, ctx 262144, Q4_K_M. Capabilities: completion, tools. Надёжный function calling, 26+ tools. Backup при Gemma edge cases (ADR-012). Лучший SQL executor (11A: SA 75–90%, latency 0.4s). |
| 5 | qwen3.5:9b | 6.6 ГБ | Fast chat, **commit-msg**, light tools | **Active** | Tool calling восстановлен в Ollama 0.20.0 |
| 6 | deepseek-r1:32b | 19 ГБ | Deep math/reasoning | **Active** | Специализированный reasoner |
| 7 | qwen3:30b | 18 ГБ | General / Planner reserve | **Active** | Понижен: gemma4:31b забрала роль Planner |
| 8 | qwen3.5:35b | 23 ГБ | Experimental reasoning | **Legacy** | Tools СЛОМАНЫ в Ollama. НЕ использовать как Agent/Planner. |
| 9 | qwen2.5-coder:7b | 4.7 ГБ | **Autocomplete FIM** (резидентная) | **Active** | arch=qwen2, 7.6B, ctx 32768, Q4_K_M. Capabilities: completion, tools, insert (FIM). |
| 10 | qwen3-embedding | 4.7 ГБ | **Embeddings backend + IDE + RAG** | **Active** | arch=qwen3, 7.6B, ctx 40960, embedding dim 4096, Q4_K_M. Capabilities: embedding only. Backend через gateway (9A) + Continue.dev (9B) + RAG MCP (11). **Инвариант:** смена модели или embedding dim требует полной переиндексации ChromaDB. |
| 11 | qwen3-vl:8b | 6.1 ГБ | Vision reserve | **Reserve** | Понижен: gemma4:26b/31b имеют vision |
| 12 | glm-4.7-flash | 19 ГБ | Agentic coding reserve | **Reserve** | Понижен: gemma4:26b занимает нишу fast agent |
| 13 | deepseek-r1:14b | 9.0 ГБ | Math/reasoning компактная | **Reserve** | |
| 14 | qwen3:14b | 9.3 ГБ | Chat резерв | **Reserve** | |
| 15 | qwen2.5-coder:1.5b | 986 МБ | Autocomplete FIM fallback | **Reserve** | |

> **Примечание:** Диск-резидентные модели (Reserve, Legacy) не потребляют вычислительных ресурсов. qwen3-embedding используется через endpoint, но не резидентна постоянно. Runtime-параметры (architecture, parameters, ctx, quantization, capabilities) для Tier 1, embedding и FIM моделей верифицированы через `ollama show` на 2026-04-05.

---

## 6. HTTP-шлюз (gateway v0.12.0)

Место: `~/llm-gateway/gateway/` (Python-пакет, 11 модулей). Entrypoint: `~/llm-gateway/run.py`. FastAPI + Uvicorn + httpx. Systemd: `llm-gateway.service`. Адрес: `http://192.168.0.128:8000`.

**Структура пакета (Этапы 10A + 10B + 14B + 16):**

| Модуль | Назначение |
|--------|-----------|
| `__init__.py` | VERSION, константы конфигурации |
| `app.py` | FastAPI assembly: middleware, handlers, routers |
| `auth.py` | Per-user Bearer token auth (14B) |
| `models.py` | Pydantic/dataclass модели запросов/ответов |
| `errors.py` | GatewayError + exception handlers |
| `upstream.py` | httpx client, retry, OOM classification |
| `chat.py` | /v1/chat/completions + helpers |
| `embeddings.py` | /v1/embeddings + helpers |
| `listing.py` | /v1/models, /health |
| `logging_config.py` | JSONFormatter, structured logging |
| `metrics.py` | MetricsCollector, /metrics endpoint |
| `orchestrate.py` | /v1/orchestrate + /v1/orchestrate/pipelines (16) |

### 6.1. Эндпоинты

| Метод | Путь | Назначение | Статус |
|---|---|---|---|
| `POST` | `/v1/chat/completions` | OpenAI-совместимый | **Active** |
| `GET` | `/v1/models` | Список моделей | **Active** |
| `GET` | `/health` | Состояние шлюза | **Active** |
| `POST` | `/v1/embeddings` | Embeddings через Ollama (qwen3-embedding, dim 4096) | **Active** (этап 9A ✅) |
| `GET` | `/metrics` | JSON метрики (in-memory counters) | **Active** (этап 10B ✅) |
| `POST` | `/v1/orchestrate` | Sequential multi-model pipeline (6 pipelines, self-call) | **Active** (этап 16 ✅) |
| `GET` | `/v1/orchestrate/pipelines` | Список доступных pipeline | **Active** (этап 16 ✅) |

### 6.2. Ключевые возможности

- Настоящий стриминг (TTFT — Time To First Token = 0.12s)
- Reasoning policy (effort → think: true/false на стороне шлюза)
- Depth-over-Speed (num_ctx 8192/32768, MAX_NUM_CTX=131072 после этапа 15)
- Валидация (Pydantic), OOM (Out of Memory)-детекция (503), retry холодного старта
- OpenAI SDK совместимость (system_fingerprint, service_tier, extra="ignore", vision, tools)
- Tool_calls проброс Ollama → OpenAI формат (stream + non-stream)
- **Bearer auth (mandatory per-user, Этап 14B):** env LLM_GATEWAY_TOKENS (JSON {token: user_id}), 3 пользователя (vladimir, roadwarrior, orchestrator). /health и /metrics exempt. user_id в каждом structured log event (audit trail). gateway отказывается стартовать без токенов.
- **Embeddings endpoint** `/v1/embeddings`: OpenAI-compatible, batch support, allowlist models, staged pipeline validation, encoding_format silent coerce (9B hotfix)
- **Structured JSON logging** (Этап 10A): каждый запрос → JSON event в systemd journal. События: `llm_completion` (chat с tokens, latency_ms, ttft_ms, tool_calls_count, reasoning_effort), `llm_embedding` (embed с dim, latency_ms), `llm_orchestrate` (pipeline с steps_count, total_duration). Custom JSONFormatter (stdlib, ноль зависимостей). uvicorn.access отключён — свой middleware. Парсинг: `journalctl -o cat | jq -R 'fromjson? | select(.)'`
- **In-memory metrics** (Этап 10B): `GET /metrics` — JSON-снимок агрегированных счётчиков. Три группы: totals (requests, errors, uptime), endpoints (per-path tokens, latency_ms_sum/count), models (per-model requests/tokens/errors). Счётчики монотонные, сбрасываются при рестарте. Без auth (как /health). Thread-safe (threading.Lock)
- **Orchestration endpoint** (Этап 16): `POST /v1/orchestrate` — sequential multi-model pipeline через self-call. Каждый шаг = POST /v1/chat/completions на localhost с service token `orchestrator`. Auth, logging, metrics для каждого шага автоматически. 6 pipeline из `pipelines.yaml`. Structured log: `llm_orchestrate` event. `GET /v1/orchestrate/pipelines` — pipeline registry API.

### 6.3. Архитектурный roadmap gateway

| Версия | Что добавляется | Этап |
|--------|----------------|------|
| v0.7.0 | Стриминг, reasoning, tool_calls, auth | ✅ 7A |
| **v0.8.0** | **/v1/embeddings (qwen3-embedding, batch, allowlist)** | ✅ **9A** |
| **v0.9.0** | **Structured JSON logging + модуляризация в пакет** | ✅ **10A** |
| v0.10.0 | /metrics endpoint | ✅ **10B** |
| **v0.11.0** | **Per-user mandatory auth + audit trail** | ✅ **14B** |
| **v0.12.0** | **/v1/orchestrate + /v1/orchestrate/pipelines (self-call pattern)** | ✅ **16** |

---

## 7. IDE-интеграция: Continue.dev v1.2.22 + Copilot BYOK

### 7.1. Архитектура подключения

```
VS Code 1.112 (Windows 11)
  ├── Continue.dev v1.2.22 (ОСНОВНОЙ)
  │     ├── Chat/Edit/Agent → gateway.py :8000 /v1/chat/completions → Ollama :11434
  │     ├── Model list     → gateway.py :8000 /v1/models
  │     ├── Autocomplete   → Ollama :11434 напрямую (FIM, /api/generate)
  │     ├── Embeddings     → gateway.py :8000 /v1/embeddings → Ollama (qwen3-embedding, 4096d)  ← 9B
  │     ├── MCP (Agent)    → STDIO → uvx mcp-server-git (Windows)     ← 8A
  │     ├── MCP (Agent)    → streamable-http → rag-mcp-server (Ubuntu :8100)  ← 11
  │     └── @Codebase      → embeddings vector search (deprecated provider)   ← 9B
  │
  └── Copilot BYOK (ДОПОЛНИТЕЛЬНЫЙ, plain chat only)
        ├── Путь A (Ollama) → Ollama :11434 напрямую
        └── Путь C (OAI Compatible ext) → gateway.py :8000 → Ollama :11434
```

### 7.2. Работающие сценарии

| Сценарий | Модель | Статус | Этап |
|----------|--------|--------|------|
| **Chat (quality-first)** | **gemma4:31b** | **✅** | **10D** |
| **Chat (fast semantic)** | **gemma4:26b** | **✅** | **10D** |
| Chat (повседневный) | qwen3.5:9b | ✅ | 7B |
| Chat (reasoning) | deepseek-r1:32b | ✅ | 7B |
| Chat (experimental) | qwen3.5:35b | ✅ | 7A |
| **Edit / Refactor** | **gemma4:31b / gemma4:26b / qwen3-coder:30b** | **✅** | **10D** |
| **Agent mode (tools)** | **gemma4:31b** | **✅** | **10D** |
| **Agent mode (tools)** | **gemma4:26b (zero-shot)** | **✅** | **10D** |
| Agent mode (tools) | qwen3-coder:30b | ✅ | 7A |
| Agent mode (tools) | glm-4.7-flash | ✅ | 7B |
| Vision | gemma4:31b / gemma4:26b / qwen3-vl:8b | ✅ | 7A, 10D |
| Autocomplete (FIM) | qwen2.5-coder:7b | ✅ | 7B |
| Apply на существующем файле | gemma4:31b / gemma4:26b / qwen3-coder:30b | ✅ | 7C, 10D |
| Context: @Code / @Repository Map | qwen3-embedding (index), tree-sitter (AST) | ✅ | 7C, 9B |
| **Embeddings: @Codebase (index)** | **qwen3-embedding via gateway** | **✅ (deprecated provider)** | **9B** |
| Slash commands (/review и др.) | Любая chat-модель | ✅ | 7C |
| Copilot BYOK plain chat | qwen3-coder:30b | ✅ | 7D |
| Copilot Agent mode (tools) | qwen3-coder:30b | ❌ нестабилен | 7D |
| **MCP Git: status, log, diff** | **gemma4:31b / gemma4:26b / qwen3-coder:30b** | **✅** | **8A, 10D** |
| **MCP Git: add, commit, branch** | **qwen3-coder:30b** | **✅** | **8A** |
| **MCP RAG: search_docs** | **gemma4:31b / gemma4:26b / qwen3-coder:30b** | **✅** | **11** |
| **MCP RAG: list_collections, index_status** | **gemma4:31b / gemma4:26b / qwen3-coder:30b** | **✅** | **11** |
| **MCP cross-tool: git + RAG** | **gemma4:31b / gemma4:26b** | **✅** | **11** |

### 7.3. MCP Tool Layer (Слой 3) — компоненты и статусы

MCP — Model Context Protocol (протокол контекста модели) — открытый стандарт Anthropic для подключения AI к внешним инструментам.

| # | MCP Server | Версия | Транспорт | Tools | Статус | Этап |
|---|-----------|--------|-----------|-------|--------|------|
| 1 | **mcp-server-git** (Anthropic) | 2026.1.14 | STDIO (uvx) | 12 | **Active** | ✅ 8A |
| 2 | **Terminal Policy** (rules-based, Continue Agent на Windows) | — | Нативный (VS Code Terminal) | — | **Active** | ✅ 8B |
| 3 | **Lab RAG Server** (custom, FastMCP) | mcp 1.27.0 | streamable-http (порт 8100) | 5 (search_docs, get_doc_section, grep_doc, list_collections, index_status) | **Active** | ✅ 11 |
| 4 | Docker MCP | — | streamable-http | — | **Planned** | 12 |

Конфигурации MCP:
- `%USERPROFILE%\.continue\mcpServers\git.yaml` — Git (STDIO)
- `%USERPROFILE%\.continue\mcpServers\rag.yaml` — RAG (streamable-http)

Инструментарий клиента: **uv 0.10.12** (Astral) — `uvx` для запуска MCP-серверов STDIO.

> **Ограничение:** MCP-серверы работают ТОЛЬКО в Agent mode. В Chat и Edit не доступны.

> **ADR-014:** Серверные MCP (нужен доступ к ресурсам Ubuntu) используют streamable-http. Клиентские MCP (работают на Windows) используют STDIO. SSE deprecated в MCP spec.

### 7.4. Конфигурация Continue

Файл: `%USERPROFILE%\.continue\config.yaml`.

**Модели (10):** 1 embed (qwen3-embedding через gateway) + 1 autocomplete (qwen2.5-coder:7b через Ollama) + 8 chat-capable (через шлюз), организованы по Tier (ADR-012): Tier 1 — gemma4-31b, gemma4-26b, qwen3-coder; Tier 2 — qwen35-9b, deepseek-r1; Tier 3 — qwen35, glm-flash, qwen3-vl.

**Context providers (12):** code, codebase (deprecated), repo-map, file, currentFile, open, diff, terminal, tree, problems, clipboard, os.

**Prompts (6):** /review, /refactor, /docstring, /commit, /explain, /tests.

**Rules:** Глобальные (01-general.md, 02-coding.md, **03-security.md**) + проектные (01-project.md).

**MCP Servers:** git (STDIO, uvx mcp-server-git, 12 tools), **Lab RAG** (streamable-http, http://192.168.0.128:8100/mcp, 5 tools).

### 7.5. Грабли Continue 1.2.22 (полный список)

**Конфигурация:**
- **timeout** — в миллисекундах (OpenAI SDK). 600000 = 10 минут для chat, 30000 = 30 сек для autocomplete
- **extraBodyProperties** — ВНУТРИ `requestOptions`, НЕ на уровне модели
- **YAML anchors** (`<<: *defaults`) — НЕ принимает
- **Имена моделей со скобками** — вызывает `Invalid input`
- **roles: agent** — невалидная роль
- **tabAutocompleteModel** (top-level) — не существует в YAML schema v1
- **debounce → debounceDelay** в YAML-схеме
- **Дефолтный таймаут autocomplete ~5 сек** — мало для холодного старта
- **Секция context:** сбрасывает дефолтные провайдеры — все перечислять явно
- **Проектные rules** — требуют `Developer: Reload Window` при первом создании
- **capabilities: [tool_use]** — НЕ требуется, Continue автодетектит
- **MCP серверы работают ТОЛЬКО в Agent mode** — не в Chat, не в Edit

**Embeddings (9B):**
- **encoding_format: float обязателен** — Continue `provider: openai` отправляет несовместимый encoding_format. Fix: `requestOptions.extraBodyProperties.encoding_format: float`
- **apiKey заглушка обязательна** — Continue `provider: openai` требует непустой apiKey даже без auth. Fix: `apiKey: ollama`
- **Rebuild codebase index → EBUSY на Windows** — штатная команда не может удалить `index.sqlite`. Fix: cold restart VS Code + ручное переименование `%USERPROFILE%\.continue\index\`
- **@Codebase provider deprecated** — помечен deprecated в документации Continue; Agent mode built-in tools рекомендуется как замена. Deprecated provider даёт нестабильные результаты (synthetic code вместо grounded retrieval)

**Модели:**
- **qwen3.5:35b** — НЕ поддерживает function calling в Ollama (НИКОГДА для Agent), НЕ использовать как Planner в orchestrator
- **qwen3-coder:30b** — надёжный Agent, работает с 26+ tools (12 MCP + 14 built-in)
- **gemma4:31b** — Agent mode работает, но для оптимального tool selection рекомендуется thin policy в system prompt (ADR-012, 10C)
- **gemma4:26b** — лучший zero-shot tool selector в Agent mode (ADR-012, 10C, 10D)

**Windows / Terminal (Continue Agent):**
- **PowerShell-native обязателен** — Agent по умолчанию может генерировать bash-style (`ls -la`); фиксируется через 01-general.md (alwaysApply)
- **UTF-8 preamble** — для корректного вывода кириллических имён файлов необходимо выставлять кодировки в начале команды
- **Occasional quoted-command** — редкий дефект: модель иногда оборачивает команду в строку и печатает вместо выполнения; not blocking, known issue
- **Reload Window + новый чат** — обязательный цикл проверки при изменении rules

### 7.6. Компоненты IDE по статусам

| Компонент | Версия | Статус | Примечание |
|-----------|--------|--------|-----------|
| VS Code | 1.112 | **Active** | |
| Continue.dev | v1.2.22 | **Active** | Основной agent runtime. Documentation drift: ранее 1.2.17 в документах. |
| uv/uvx (Astral) | 0.10.12 | **Active** | Запуск MCP-серверов |
| OAI Compatible Provider | — | **Active** | Copilot BYOK через шлюз |
| GitHub Copilot (Free) | — | **Active** | Plain chat only |
| Copilot Agent mode с локальными моделями | — | **Legacy** | Нестабилен, не развивать |

### 7.7. Copilot BYOK (Этап 7D)

GitHub Copilot Free plan + VS Code 1.112. Plain chat работает (Ollama напрямую и через шлюз). Agent mode нестабилен с локальными моделями.

---

## 8. Слой 4: Orchestration / Automation

### 8.1. orchestrator.py v1.1.0 (Этапы 8C + 8D) — Active (standalone CLI)

**Расположение:** `~/llm-gateway/orchestrator.py`  
**Среда выполнения:** `~/llm-gateway/venv/bin/python` (обязательно через venv — пакет `openai` только там)  
**Вызывает:** gateway `/v1/chat/completions` через OpenAI SDK

**Назначение:** Sequential multi-model pipeline (CLI). Сохраняет результаты в `~/llm-gateway/results/`.

**CLI:**
```bash
./venv/bin/python orchestrator.py --help
./venv/bin/python orchestrator.py --list
./venv/bin/python orchestrator.py --pipeline <n> --task "<text>"
./venv/bin/python orchestrator.py --pipeline <n> --task "<text>" --stdout
```

**Флаг `--stdout` (добавлен в 8D):** при наличии — текст последнего шага pipeline печатается в `sys.stdout` (для shell-интеграции). JSON в `results/` при этом тоже сохраняется.

**Реализованные pipeline (`~/llm-gateway/pipelines.yaml`, обновлены в 10D):**

| Pipeline | Шаги | Модели | Типичное время |
|----------|------|--------|---------------|
| `smoke` | smoke | qwen3.5:9b | ~5 сек |
| `execute-review` | executor → reviewer | **gemma4:26b → gemma4:31b** | ~60–90 сек (первый запуск) |
| `plan-execute-review` | planner → executor → reviewer | **gemma4:31b → gemma4:26b → gemma4:31b** | U (не замерен после 10D) |
| `review-only` | reviewer | **gemma4:31b** | U (не замерен после 10D) |
| `docs-generate` | executor | **gemma4:26b** | U (не замерен после 10D) |
| `commit-msg` | executor | qwen3.5:9b | ~5–10 сек |

**Ключевые технические особенности:**
- Фильтрация `<think>...</think>` блоков по имени шага `planner`
- Результаты: `~/llm-gateway/results/YYYYMMDD_HHMMSS.json`
- `temperature: 0.2` для Executor/Reviewer; `0.4` для Planner; `0.1` для commit-msg
- Запуск только через `venv/bin/python` — `python3` без venv даст `ModuleNotFoundError: openai`

**Известные ограничения:**
- qwen3.5:35b нестабилен как Planner. Planner = gemma4:31b (ранее qwen3:30b).
- `datetime.utcnow()` DeprecationWarning на Python 3.12+ (Грабли #34). Не блокирующее.

### 8.1a. gateway /v1/orchestrate (Этап 16) — Active (HTTP API)

**Модуль:** `~/llm-gateway/gateway/orchestrate.py`
**Эндпоинты:** `POST /v1/orchestrate`, `GET /v1/orchestrate/pipelines`

**Назначение:** Формализация orchestrator паттерна в gateway API. Те же 6 pipeline из `pipelines.yaml`, но доступны через HTTP API для программного вызова из IDE, MCP, скриптов.

**Архитектурный паттерн — self-call (ADR-016):** каждый шаг pipeline → `POST /v1/chat/completions` на `localhost:8000` с service token `orchestrator`. Auth, structured logging, metrics — автоматически для каждого шага, без дублирования кода.

**API-контракт:**
```
POST /v1/orchestrate
Body: {"pipeline": "smoke", "task": "Hello world"}
Response: {"pipeline": "smoke", "steps": [...], "total_duration_sec": 3.67, "user_id": "vladimir", "timestamp": "..."}
```

**Structured log:** event `llm_orchestrate` (pipeline-level, user_id вызывающего) + events `llm_completion` (step-level, user_id=orchestrator, client_ip=127.0.0.1).

**/health response:**
```json
{
  "gateway": "ok",
  "version": "0.12.0",
  "ollama": "ok",
  "models_count": 15,
  "pipelines_count": 6,
  "auth_enabled": true,
  "user_count": 3
}
```

### 8.2. Headless Scripts + Git Hooks (Этап 8D) — Active

**Расположение:** `~/llm-gateway/scripts/`, `~/llm-gateway/.githooks/`

**Паттерн:** shell context capture → orchestrator LLM pipeline → stdout.

| Скрипт | Назначение | Pipeline | Выход |
|--------|-----------|---------|-------|
| `scripts/auto-review.sh` | Code review по git diff | `execute-review` | stdout, exit 0 всегда |
| `scripts/auto-commit-msg.sh` | Conventional commit message | `commit-msg` | однострочный stdout |
| `scripts/auto-docs.sh` | Документация файла | `docs-generate` | stdout |
| `.githooks/pre-push` | Review перед push | `execute-review` (через auto-review.sh) | exit 0 / exit 1 (STRICT) |

**Режимы auto-review.sh:**
- `--staged` (default) → `git diff --cached`
- `--last-commit` → `git diff HEAD~1 HEAD`
- `--branch <n>` → `git diff <n>...HEAD`

**Режимы pre-push хука:**
- Default: review выводится, push продолжается
- `STRICT_REVIEW=1` + TTY: интерактивное подтверждение
- `STRICT_REVIEW=1` + non-TTY: fail-closed, exit 1 (безопасно для CI/CD)

**Активация:**
```bash
cd ~/llm-gateway
git config core.hooksPath .githooks
chmod +x .githooks/pre-push scripts/auto-review.sh scripts/auto-commit-msg.sh scripts/auto-docs.sh
```

**Ограничения (задокументированные):**
- `auto-docs.sh --format docstring`: модель возвращает обзорную документацию, а не код с патчем docstrings. Приемлемо для PoC.
- `auto-review.sh --branch`: использует имя ветки, не SHA. Для production — `REMOTE_SHA..LOCAL_SHA`.
- `STRICT_REVIEW=1` без TTY всегда fail-closed в текущей SSH-среде.

**MAX_LINES=400** в auto-review.sh и auto-commit-msg.sh — защита от превышения num_ctx при больших диффах.

> **Примечание (10D):** После Config Sync headless scripts `auto-review.sh` и `auto-docs.sh` теперь фактически используют Gemma 4 через обновлённые pipeline (`execute-review` → gemma4:26b→gemma4:31b, `docs-generate` → gemma4:26b). Скрипты `auto-commit-msg.sh` и `pre-push` не затронуты (используют `commit-msg` и `execute-review` pipeline соответственно).

### 8.3. Text-to-SQL PoC (Этап 11A) — Track F: AI-as-Interface

**Расположение:** `~/llm-gateway/scripts/text2sql_*.py`, `~/llm-gateway/ondo_demo.db`

**Назначение:** Benchmark NL→SQL (Natural Language → SQL) на локальном контуре. Первый PoC паттерна AI-as-Interface.

**Стек:**
- SQLite (`ondo_demo.db`): 6 таблиц, 171 запись, схема ONDO (departments, employees, facilities, energy_consumption, hotel_bookings, production_orders)
- Python-скрипт через OpenAI SDK → gateway → Ollama
- Read-only enforcement: SQLite `PRAGMA query_only = ON` + regex-валидация
- 20 тестовых вопросов (4 уровня: simple, join+groupby, subquery, edge cases + safety + hallucination)

**Benchmark results (baseline truth, v2 evaluator):**

| Модель | EA | SA | Latency |
|--------|----|----|---------|
| **qwen3-coder:30b** | 100% | 75% | 0.4s |
| gemma4:31b | 95% | 70% | 4.0s |
| gemma4:26b | 95% | 65% | 1.5s |

**Best observed (v3, + prompt addendum):**

| Модель | EA | SA | Дельта SA |
|--------|----|----|-----------|
| qwen3-coder:30b | 100% | 90% | +15 п.п. |
| gemma4:31b | 100% | 90% | +20 п.п. |
| gemma4:26b | 100% | 85% | +20 п.п. |

**Статус:** PoC завершён. Следующий шаг: semantic grounding / retrieval-assisted SQL.

### 8.4. RAG MCP Server (Этап 11) — Active

**Расположение:** `~/rag-mcp-server/`
**Среда выполнения:** `~/rag-mcp-server/.venv/bin/python`
**Systemd:** `rag-mcp.service`
**Порт:** 8100 (streamable-http)

**Назначение:** Семантический поиск по документации лаборатории через MCP. Первый custom серверный MCP-сервер в стеке.

**Tools:**

| Tool | Назначение | Параметры |
|------|-----------|-----------|
| `search_docs` | Семантический поиск | query, top_k (default 5) |
| `get_doc_section` | Получить секцию документа | doc_id, section |
| `grep_doc` | Текстовый поиск | pattern |
| `list_collections` | Список коллекций | — |
| `index_status` | Статус индекса | — |

**Vector DB:** ChromaDB 1.5.5, PersistentClient, `~/rag-data/chroma/`, collection `lab_docs`.

**Корпус:** 12 файлов / 12 chunks (ADR-only primary: 5 foundational + 7 canonical). Индексация — batch (offline): stop service → `set -a && source ~/llm-gateway/.env && set +a` → python3 indexer.py → start service (ChromaDB PersistentClient — один процесс). Non-ADR контент (gateway code, scripts, results) вынесен в backup-зоны (docs_nonadr_backup, docs_secondary_backup, docs_legacy_backup) — осознанное решение: RAG = экспериментальный полигон для ADR retrieval.

**Data path:** Agent query → RAG MCP (8100) → gateway (8000) /v1/embeddings → Ollama → qwen3-embedding → 4096d vector → ChromaDB similarity → grounded results.

**Качество retrieval (Agent mode, 10 запросов):** 9/10 релевантно, кириллица работает, cross-tool (git+RAG) подтверждён.

**ADR-014:** streamable-http для серверных MCP (не STDIO, не SSE).

### 8.5. Health Automation + Benchmark Matrix (Этап 15) — Active

**Расположение:** `~/llm-gateway/scripts/health-check.sh`, `~/llm-gateway/scripts/benchmark.py`
**Baseline:** `~/llm-gateway/benchmarks/`

**Назначение:** Regression guardrail для стека. Воспроизводимая проверка после обновлений.

**health-check.sh** — 11 проверок критических компонентов, exit 0 (all OK) / exit 1 (failures). Проверяет: Ollama alive + model count, gateway alive + version + /metrics, embeddings endpoint + dim 4096, rag-mcp.service, UFW active + ports, disk space.

**benchmark.py** — 7 сценариев через gateway: chat quality (gemma4:31b), chat fast (gemma4:26b), chat executor (qwen3-coder:30b), reasoning (deepseek-r1:32b), embeddings (qwen3-embedding), orchestrator smoke, Text-to-SQL regression. Temperature 0 для воспроизводимости.

**128K Context Window finding:**
- gemma4:31b и gemma4:26b при ctx ≥ 65536 — stall после загрузки (GPU util = 0%, генерация не начинается)
- Root cause: upstream bug в Ollama 0.20.0 / llama.cpp — SWA архитектура Gemma 4 ломает context-shift (llama.cpp #21379)
- Статус: Blocked/External. Отслеживать Ollama 0.20.1+. Стабильная граница: ctx ≤ 32768 для Gemma 4

**Gateway fixes (побочный результат этапа 15):**
- `MAX_NUM_CTX`: 32768 → 131072 (gateway/models.py)
- `HTTPX_TIMEOUT`: 600s → 3600s (gateway/__init__.py)

---

## 9. Security / Network Perimeter (Этап 14)

### 9.1. UFW Firewall (14A)

Статус: **Active**, default deny incoming.

| # | Порт | Протокол | Доступ | Назначение |
|---|------|----------|--------|-----------|
| 1 | 22/tcp | SSH | LAN 192.168.0.0/24 + WG 10.10.10.2 | Управление |
| 2 | 8000/tcp | Gateway | LAN + WG | API шлюз (mandatory auth) |
| 3 | 11434/tcp | Ollama | 192.168.0.164 + 10.10.10.2 | FIM autocomplete (конкретные клиенты) |
| 4 | 8100/tcp | RAG MCP | LAN + WG | Семантический поиск |

Ollama НЕ доступен всему LAN — только whitelist клиентов (FIM latency bypass). Все остальные запросы — через gateway.

### 9.2. Gateway Auth (14B + 16)

Модель: **mandatory per-user Bearer tokens**.

Хранение: `~/llm-gateway/.env` (chmod 600), формат: `LLM_GATEWAY_TOKENS='{"token": "user_id"}'` (single-quoted для shell safety, Грабли #49).
Подключение: `EnvironmentFile=` в systemd override (`llm-gateway.service` и `rag-mcp.service`).

Пользователи: `vladimir` (Windows LAN), `roadwarrior` (WG road-warrior), `orchestrator` (service token для pipeline self-calls, Этап 16).
Exempt paths: `/health`, `/metrics`.

Audit: поле `user_id` в каждом structured JSON log event (`llm_completion`, `llm_embedding`, `llm_orchestrate`).

### 9.3. Ops Automation (14A)

- **Nightly health-check:** `~/llm-gateway/scripts/health-check.sh` (11/11 проверок), cron 06:00 Kyiv → `~/llm-gateway/logs/nightly-health.log`
- **Benchmark history:** `~/llm-gateway/scripts/benchmark-with-history.sh` — immutable snapshots + `~/llm-gateway/benchmarks/history.jsonl`
- **Ollama Canary SOP:** `~/llm-gateway/docs/SOP_Ollama_Upgrade.md` — 6-step procedure, проверена на 0.20.0 → 0.20.2

### 9.4. Сетевая топология

```
Internet
  │
  ▼
MikroTik hAP ax2 (edge router)
  ├── WireGuard endpoint
  │     └── 10.10.10.2 — road-warrior Ubuntu Desktop
  │
  └── LAN 192.168.0.0/24
        ├── 192.168.0.128 — LLM Server (Ubuntu 24.04, RTX 3090)
        └── 192.168.0.164 — Windows 11 client (VS Code + Continue.dev)
```

---

## 10. Systemd-сервисы

| Сервис | Назначение | Статус |
|---|---|---|
| `ollama.service` + override | Ollama 0.20.2 | **Active** |
| `llm-gateway.service` | Шлюз v0.12.0, автостарт после Ollama, EnvironmentFile=~/.env | **Active** |
| **`rag-mcp.service`** | **RAG MCP Server, after llm-gateway, EnvironmentFile=~/.env** | **Active** (11, 14B) |

---

## 11. Текущее состояние и план работ

### Завершено

| Этап | Дата | Что сделано |
|---|---|---|
| 1 — Диагностика | 2026-03-15 | 8 проблем, карта рисков |
| 2 — Reasoning + Depth | 2026-03-15 | v0.2.0, reasoning policy, Depth-over-Speed |
| 3 — Параметры генерации | 2026-03-15 | v0.3.0, валидация, penalties, логирование |
| 4 — Обработка ошибок | 2026-03-15 | v0.4.0, HTTP-коды, OOM, retry |
| 5 — API-контракт | 2026-03-16 | v0.5.0, OpenAI SDK совместимость, auth, tools |
| 6 — Настоящий стриминг | 2026-03-16 | v0.6.0, TTFT 0.12 сек |
| 7A — Continue Chat + Agent | 2026-03-16 | v0.7.0, tool_calls проброс, GGML_CUDA_NO_GRAPHS |
| 7B — Autocomplete + Стек | 2026-03-17 | Ollama 0.18.0, 4 новых модели, FIM, MAX_LOADED_MODELS=2 |
| 7C — Context + Rules + Prompts | 2026-03-19 | Embeddings (transformers.js), 11 context, rules, 6 prompts, Apply |
| 7D — Copilot BYOK | 2026-03-19 | Plain chat работает; Agent mode нестабилен |
| **8A — MCP: Git Server** | **2026-03-21** | **mcp-server-git (STDIO, uvx, 12 tools). PoC MCP-интеграции подтверждён.** |
| **8B — Terminal Policy Framework** | **2026-03-27** | **Terminal Policy Baseline для Continue Agent на Windows. 01-general.md (alwaysApply). PowerShell-native, UTF-8 preamble.** |
| **8C — Orchestrator PoC** | **2026-03-27** | **orchestrator.py + pipelines.yaml. 5 pipeline. Planner=qwen3:30b, Executor=qwen3-coder:30b, Reviewer=deepseek-r1:32b. Время plan-execute-review: 53–88 сек.** |
| **8D — Headless Automation PoC** | **2026-03-27** | **orchestrator.py v1.1.0 (+--stdout). pipelines.yaml +commit-msg. scripts/auto-review.sh, auto-commit-msg.sh, auto-docs.sh. .githooks/pre-push с STRICT_REVIEW режимом. Все 7 сценариев подтверждены на реальном железе.** |
| **9A — gateway /v1/embeddings** | **2026-03-28** | **gateway.py v0.8.0. POST /v1/embeddings: OpenAI-compatible, batch, allowlist (qwen3-embedding). qwen3-embedding: Active (backend).** |
| **9B — Embeddings миграция Continue** | **2026-03-29** | **Continue embeddings: transformers.js → qwen3-embedding через gateway. Transport path подтверждён. encoding_format hotfix. provider: codebase добавлен. Continue 1.2.22 (documentation drift). 4 грабли зафиксированы.** |
| **10A — Structured Logging + Модуляризация** | **2026-03-31** | **gateway.py v0.8.0 → v0.9.0. Монолит (1305 строк) разбит на пакет gateway/ (8 модулей). Structured JSON logging: llm_completion (chat) + llm_embedding (embed) events. Custom JSONFormatter (stdlib). uvicorn.access → свой middleware. run.py entrypoint. systemd обновлён. ADR-008 закрыт. Docstring MAX_LOADED_MODELS=2 исправлен. 2 грабли (systemd placeholder, scp mkdir).** |
| **10B — /metrics endpoint** | **2026-04-03** | **gateway v0.9.0 → v0.10.0. Новый модуль metrics.py: MetricsCollector (singleton, thread-safe). GET /metrics: JSON-счётчики по totals, endpoints, models. Интеграция: collector.record(log_data) в 8 точках (5 chat + 3 embed). /metrics освобождён от Bearer auth (AUTH_EXEMPT_PATHS). Regression: structured logging 10A не нарушен. 1 грабля (#32: deployment file mismatch).** |
| **10C — Ollama Upgrade + Gemma 4** | **2026-04-03** | **Ollama 0.18.0 → 0.20.0. +3 модели Gemma 4 (31b, 26b, e4b): 13→16. Раскладка ролей радикально обновлена (ADR-012): gemma4:31b=Quality-first Agent/Planner/Reviewer, gemma4:26b=Fast Semantic Agent/Executor, qwen3-coder:30b=Stable Generic Executor/Backup. Tool calling (generic+semantic+multi-step) подтверждён. deepseek-coder-v2→Deprecated. qwen3.5:9b tool calling восстановлен. Грабли #33 (override.conf backup).** |
| **10D — Config Sync** | **2026-04-04** | **Continue config.yaml: 8→10 моделей (+ gemma4-31b, gemma4-26b), Tier-организация (ADR-012). pipelines.yaml: 4 pipeline обновлены на Gemma 4 (plan-execute-review, execute-review, review-only, docs-generate). End-to-end подтверждено: Chat, Agent mode (MCP tool use), orchestrator execute-review, gateway /metrics delta. Грабли #34 (datetime.utcnow DeprecationWarning).** |
| **11A — Text-to-SQL PoC** | **2026-04-04** | **NL→SQL PoC доказан на SQLite (6 таблиц, 171 запись, ONDO-тематика). 20 вопросов × 3 модели. Baseline truth (v2 evaluator): qwen3-coder:30b SA 75%, gemma4:31b SA 70%, gemma4:26b SA 65%. Best (v3 + prompt addendum): SA 85–90% (все модели, +15–20 п.п., model-agnostic). Safety/hallucination: 100% pass. qwen3-coder:30b — лучший SQL executor (0.4s, EA 100%). Prompt bleed зафиксирован (Грабли #36). ADR-013: evaluator separation + prompt freeze. Грабли #35–37.** |
| **11 — MCP: RAG / Docs Search** | **2026-04-04** | **Custom RAG MCP server (FastMCP, mcp 1.27.0, streamable-http, порт 8100). ChromaDB 1.5.5 PersistentClient: 133 файла / 297 chunks, collection lab_docs. Embedding: qwen3-embedding (4096d) через gateway /v1/embeddings. 5 tools: search_docs, get_doc_section, grep_doc, list_collections, index_status. Continue Agent mode: 9/10 релевантных результатов, кириллица работает, cross-tool (git+RAG) подтверждён. systemd rag-mcp.service. UFW 8100/tcp LAN-only. UFW was inactive — enabled (Грабли #42). ADR-014: streamable-http для серверных MCP. Грабли #38–42.** |
| **14A — Network Perimeter + Ops** | **2026-04-06** | **UFW least-privilege: default deny, 8 правил. Ollama 11434 → whitelist (Windows + WG). Nightly health-check cron (11/11). Benchmark history wrapper (immutable snapshots + history.jsonl). Ollama Canary SOP. Ollama 0.20.0→0.20.2. Грабли #46 (baseline перезапись), #47 (heredoc).** |
| **14B — Gateway Auth + Audit** | **2026-04-07** | **gateway v0.10.0→v0.11.0. Mandatory per-user Bearer auth: 2 users, .env token storage, EnvironmentFile в systemd. user_id audit trail (8 точек log_data). auth.py (10-й модуль). /health: auth_enabled=true, user_count. RAG MCP regression: gateway_embeddings.py +Bearer, rag-mcp +EnvironmentFile. Debug prints удалены. 14/14 tests PASS. Грабли #48. ADR-015.** |
| **15 — Benchmark Matrix + Health Automation** | **2026-04-05** | **health-check.sh (11/11 проверок). benchmark.py (7/7 сценариев). Baseline snapshot создан. 128K context window: Blocked/External — upstream bug Ollama/llama.cpp SWA (Грабли #43), Gemma 4 стабильна при ctx ≤ 32768, long-context (65K+) stall hardware-agnostic. Gateway fixes: MAX_NUM_CTX 32768→131072, HTTPX_TIMEOUT 600s→3600s. Грабли #43–45.** |
| **16 — gateway /v1/orchestrate** | **2026-04-07** | **gateway v0.11.0→v0.12.0. Новый модуль orchestrate.py (11-й в пакете). POST /v1/orchestrate: sequential multi-model pipeline через self-call на localhost (OpenAI SDK + service token). GET /v1/orchestrate/pipelines: pipeline registry API. Service user `orchestrator` (3-й user в .env). /health: +pipelines_count. /metrics: endpoint `/v1/orchestrate` tracked. Structured log: `llm_orchestrate` event. Gemma 4 pipeline (execute-review: 26b→31b) подтверждён. 7 integration defects при деплое. 13/13 tests PASS. Грабли #49–51. ADR-016.** |
| **13 — Knowledge Layer** | **2026-04-08** | **03-security.md → Active (alwaysApply, denylist, git safety). ADR bridge: 7 canonical + 5 foundational ADR в RAG (12 files/12 chunks, ADR-only primary). ADR multilayer model: foundational/canonical (primary) + subsidiary/operational (secondary backup) + closed/legacy (archive). Obsidian Решения/ нормализованы. ONBOARDING.md (disaster recovery). setup-check.sh (52 проверки, сервер), setup-check.ps1 (16 проверок, клиент). Ubuntu 22.04→24.04 doc drift (#56). Ollama override восстановлен: +NUM_PARALLEL=1, +OLLAMA_HOST (#57). git.yml→git.yaml (#58). Грабли #52–58.** |

### Следующие этапы (Целевая архитектура v1.12, 5 параллельных треков + Track F)

| Трек | Этап | Содержание | Статус |
|------|------|-----------|--------|
| A (MCP) | 11 | MCP: RAG/Docs (ChromaDB + qwen3-embedding) | ✅ Complete |
| A (MCP) | 12 | MCP: Docker | ⬜ Planned |
| B (Backend) | 9A | gateway /v1/embeddings | ✅ Complete |
| B (Backend) | 9B | Embeddings миграция transformers.js → qwen3-embedding | ✅ Complete |
| B (Backend) | 10A | Structured JSON logging + модуляризация | ✅ Complete |
| B (Backend) | 10B | /metrics endpoint | ✅ Complete |
| C (Knowledge) | 13 | Rules + ADR + Onboarding пакет | ✅ Complete |
| D (Ops) | 14 | Security Hardening + Multi-user Auth | ✅ Complete |
| D (Ops) | 15 | Benchmark Matrix + Health Automation | ✅ Complete |
| E (Orchestration) | 16 | gateway /v1/orchestrate (формализация) | ✅ Complete |
| F (AI-as-Interface) | 11A | Text-to-SQL PoC | ✅ Complete |
| F (AI-as-Interface) | 11 | RAG MCP Server | ✅ Complete |

---

## 12. Файлы

### На сервере

| Файл | Описание | Статус |
|---|---|---|
| `~/llm-gateway/gateway/` | Пакет v0.12.0, 11 модулей (app, auth, chat, embeddings, listing, metrics, models, errors, upstream, logging_config, orchestrate) | **Active** |
| `~/llm-gateway/run.py` | Uvicorn entrypoint | **Active** |
| `~/llm-gateway/_old_gateway.py` | Бэкап монолита v0.8.0 | **Archive** |
| `~/llm-gateway/.env` | Token storage (chmod 600, 3 users), shared EnvironmentFile | **Active** (14B, 16) |
| `~/llm-gateway/orchestrator.py` | v1.1.0, sequential pipeline + --stdout (standalone CLI) | **Active** |
| `~/llm-gateway/pipelines.yaml` | Pipeline registry (6 pipeline, Gemma 4 — 10D) | **Active** |
| `~/llm-gateway/scripts/auto-review.sh` | Headless code review (3 режима) | **Active** |
| `~/llm-gateway/scripts/auto-commit-msg.sh` | Conventional commit message | **Active** |
| `~/llm-gateway/scripts/auto-docs.sh` | Docs generation (docstring/markdown) | **Active** |
| `~/llm-gateway/.githooks/pre-push` | Git hook: review перед push | **Active** |
| `~/llm-gateway/results/` | JSON-артефакты запусков orchestrator | **Active** |
| `~/llm-gateway/ondo_demo.db` | SQLite БД для Text-to-SQL PoC (6 таблиц, 171 запись) | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_schema.sql` | DDL + seed data для ondo_demo.db | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_questions.json` | 20 тестовых вопросов + ground truth SQL | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_poc.py` | Text-to-SQL strict evaluator (v1) | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_poc_v2.py` | Text-to-SQL business-aware evaluator (v2, baseline truth) | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_poc_v3.py` | Text-to-SQL v2 + prompt addendum wrapper | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_prompt_addendum_v1.md` | 6 блоков бизнес-семантики для NL→SQL | **Active** (11A) |
| `~/llm-gateway/scripts/text2sql_results/` | JSON-отчёты benchmark-прогонов (8 файлов) | **Active** (11A) |
| `~/llm-gateway/scripts/test_stage14b.sh` | Acceptance test Этапа 14B (15 проверок) | **Active** (14B) |
| `~/llm-gateway/scripts/test_stage16.sh` | Acceptance test Этапа 16 (13 проверок) | **Active** (16) |
| **`~/rag-mcp-server/server.py`** | **RAG MCP server (FastMCP, streamable-http, порт 8100)** | **Active** (11) |
| **`~/rag-mcp-server/gateway_embeddings.py`** | **Custom ChromaDB embedding → gateway (+Bearer auth, 14B)** | **Active** (11, 14B) |
| **`~/rag-mcp-server/chunker.py`** | **Markdown-aware text chunking** | **Active** (11) |
| **`~/rag-mcp-server/indexer.py`** | **Batch indexer (offline)** | **Active** (11) |
| **`~/rag-mcp-server/index_status.json`** | **Метаданные последней индексации** | **Active** (11) |
| **`~/rag-mcp-server/docs/`** | **ADR-only primary corpus (adr-foundational/ + adr-canonical/, 12 файлов)** | **Active** (13) |
| **`~/rag-mcp-server/docs_secondary_backup/`** | **Subsidiary + operational ADR (вне индекса)** | **Archive** (13) |
| **`~/rag-mcp-server/docs_legacy_backup/`** | **Legacy ADR + RAG_export + closed (вне индекса)** | **Archive** (13) |
| **`~/rag-mcp-server/docs_nonadr_backup/`** | **Non-ADR контент: gateway, scripts, results (вне индекса)** | **Archive** (13) |
| **`~/rag-data/chroma/`** | **ChromaDB persistent storage (lab_docs)** | **Active** (11) |
| `/etc/systemd/system/ollama.service.d/override.conf` | Ollama override | **Active** |
| `/etc/systemd/system/llm-gateway.service` | Systemd unit шлюза (+EnvironmentFile) | **Active** |
| **`/etc/systemd/system/rag-mcp.service`** | **Systemd unit RAG MCP (+EnvironmentFile)** | **Active** (11, 14B) |
| **`~/llm-gateway/scripts/health-check.sh`** | **Health check (11 проверок, exit 0/1)** | **Active** (15) |
| **`~/llm-gateway/scripts/benchmark.py`** | **Benchmark matrix (7 сценариев)** | **Active** (15) |
| **`~/llm-gateway/scripts/benchmark-with-history.sh`** | **Benchmark + immutable snapshots + history.jsonl** | **Active** (14A) |
| **`~/llm-gateway/benchmarks/baseline_2026-04-05.json`** | **Baseline snapshot JSON** | **Active** (15) |
| **`~/llm-gateway/benchmarks/baseline_2026-04-05.md`** | **Baseline summary markdown** | **Active** (15) |
| **`~/llm-gateway/benchmarks/large_context_2026-04-05.json`** | **128K test results** | **Active** (15) |
| **`~/llm-gateway/docs/SOP_Ollama_Upgrade.md`** | **Ollama Canary SOP (6 steps)** | **Active** (14A) |
| **`~/llm-gateway/docs/ONBOARDING.md`** | **Восстановление стека с нуля (навигатор по паспорту)** | **Active** (13) |
| **`~/llm-gateway/scripts/setup-check.sh`** | **Полная верификация сервера (52 проверки)** | **Active** (13) |

### На клиенте (Windows)

| Файл | Описание | Статус |
|---|---|---|
| `%USERPROFILE%\.continue\config.yaml` | **10 моделей** (Tier 1/2/3), 12 context, 6 prompts, MCP, per-user apiKey | **Active** |
| `%USERPROFILE%\.continue\rules\01-general.md` | Глобальный rule: общие + Terminal Policy | **Active** |
| `%USERPROFILE%\.continue\rules\02-coding.md` | Глобальный rule: код | **Active** |
| `%USERPROFILE%\.continue\rules\03-security.md` | Глобальный rule: безопасность (denylist, git safety, токены) | **Active** (13) |
| `<workspace>\.continue\rules\01-project.md` | Проектный rule (per-project) | **Active** |
| `<workspace>\.continue\docs\adr\000-template.md` | ADR шаблон для workspace | **Active** (13) |
| `%USERPROFILE%\.continue\mcpServers\git.yaml` | MCP Git Server (STDIO, uvx) | **Active** (переименован из git.yml, #58) |
| **`%USERPROFILE%\.continue\mcpServers\rag.yaml`** | **MCP RAG Server (streamable-http)** | **Active** (11) |
| **`%USERPROFILE%\.continue\scripts\setup-check.ps1`** | **Верификация клиента (16 проверок)** | **Active** (13) |
| VS Code `settings.json` | Copilot BYOK: ollamaEndpoint + oaicopilot.* | **Active** |

---

## 13. Политика актуальности и устаревания

### 13.1. Природа документа

Паспорт фиксирует **снимок состояния** лаборатории на дату конкретной версии. Он не является постоянной истиной — стек меняется, и паспорт должен следовать за ним.

Любое расхождение между фактическим стеком и содержимым паспорта считается **техническим долгом**. Его необходимо либо устранить (обновить паспорт), либо зафиксировать явно с указанием причины и горизонта исправления.

### 13.2. Правило ревизии версий

Обязательная ревизия и обновление паспорта инициируется при любом из следующих условий:

- Установленная версия ключевого компонента отличается от указанной в паспорте **более чем на 2 минорных релиза**.
- Компонент используется в версии **старше 6 месяцев** от даты выхода более новой стабильной версии.
- Завершён новый этап разработки, изменивший состав компонентов или их конфигурацию.
- Вышла новая версия целевой архитектуры, затрагивающая данный стенд.

Ключевые компоненты для отслеживания версий: Ubuntu, NVIDIA Driver, CUDA, Docker Engine, Ollama, gateway.py, Continue.dev, VS Code, uv/uvx.

### 13.3. Компоненты со статусами PoC, Legacy, Deprecated

- **PoC-компоненты** пересматриваются **раз в квартал**: либо переводятся в Active, либо удаляются.
- **Legacy-компоненты** требуют плана замены. При отсутствии плана в течение 2 кварталов переводятся в Deprecated.
- **Deprecated-компоненты** должны быть выведены из эксплуатации до следующей плановой ревизии паспорта.

### 13.4. Политика квантизации и KV-кэша в контексте паспорта

При изменении схемы квантизации (KV-кэш или веса модели) в основном контуре:
1. Обновить соответствующую строку в таблице 5.1 (Ollama — конфигурация).
2. Зафиксировать обоснование в ADR или комментарии к изменению.
3. Актуализировать метрики производительности в секции 4.1 целевой архитектуры.

---

## 14. Регулярная ревизия паспорта

### 14.1. Периодичность

Плановая ревизия — **раз в 3 месяца**. Внеплановая — по триггерам из раздела 13.2.

### 14.2. Связка с целевой архитектурой

- При изменении **целевой архитектуры** → выпускается новая минорная версия паспорта (не позднее 2 недель после публикации архитектуры).
- При изменениях **в паспорте**, не согласованных с архитектурой → требуется ADR или явное архитектурное решение.

### 14.3. Ответственность

Владелец лаборатории несёт ответственность за актуальность паспорта. При передаче лаборатории новому владельцу — первое действие нового владельца: пройти чек-лист актуальности (раздел 15).

---

## 15. Чек-лист актуальности (для владельца лаборатории)

### 15.1. Версии ключевых компонентов

- [ ] `cat /etc/os-release` — Ubuntu 24.04 совпадает с паспортом?
- [ ] `nvidia-smi` — версия драйвера (590.48.01) и CUDA (13.1) совпадают?
- [ ] `docker --version` — Docker Engine 28.2.2?
- [ ] `ollama --version` — Ollama 0.20.2? Если выше на 2+ минорных — ревизия паспорта.
- [ ] `cat ~/llm-gateway/gateway/__init__.py | grep VERSION` — v0.12.0?
- [ ] `ls ~/llm-gateway/gateway/` — 11 .py модулей + __pycache__?
- [ ] `ls ~/llm-gateway/run.py` — entrypoint присутствует?
- [ ] `ls ~/llm-gateway/` — orchestrator.py, pipelines.yaml, scripts/, .githooks/ присутствуют?
- [ ] В VS Code: Continue.dev версия v1.2.22? Если выше — проверить changelog на breaking changes.
- [ ] `uv --version` — 0.10.12?

### 15.2. Активность моделей

- [ ] `ollama list` — 15 моделей присутствуют? (deepseek-coder-v2:16b удалена в 14A)
- [ ] qwen3-embedding (Active, backend) — работает ли `/v1/embeddings`? `curl -s http://localhost:8000/v1/embeddings -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"model":"qwen3-embedding","input":"test"}' | jq '.object'` → "list"

### 15.3. Статусы PoC и Legacy

- [ ] Есть ли компоненты со статусом PoC старше 3 месяцев? Перевести в Active или удалить.
- [ ] Есть ли компоненты Legacy без плана замены старше 6 месяцев? Перевести в Deprecated.
- [ ] Copilot Agent mode (Legacy): статус не изменился?

### 15.4. Соответствие конфигурации описанию

- [ ] `cat /etc/systemd/system/ollama.service.d/override.conf` — KV_CACHE_TYPE=q8_0, MAX_LOADED_MODELS=2, NUM_PARALLEL=1, FLASH_ATTENTION=1?
- [ ] `curl -s http://localhost:8000/health` — шлюз отвечает OK? `version` = `0.12.0`? `pipelines_count` = `6`?
- [ ] `curl -s http://localhost:8000/metrics | jq '.totals'` — endpoint отвечает JSON? (10B)
- [ ] `curl -s http://localhost:8000/v1/embeddings -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"model":"qwen3-embedding","input":"test"}' | jq '.object'` — возвращает `"list"`?
- [ ] `curl -s http://localhost:8000/v1/embeddings -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"model":"qwen3-embedding","input":"test"}' | jq '.data[0].embedding | length'` — возвращает `4096`? (инвариант RAG)
- [ ] `journalctl -u llm-gateway -n 5 -o cat | jq -R 'fromjson? | select(.)'` — JSON парсится? (10A)
- [ ] `curl -s http://localhost:11434/api/tags | jq '.models | length'` — 15 моделей?
- [ ] `cd ~/llm-gateway && ./venv/bin/python orchestrator.py --list` — 6 pipeline доступны?
- [ ] `curl -sf -H "Authorization: Bearer $TOKEN" http://localhost:8000/v1/orchestrate/pipelines | jq '.pipelines | keys | length'` — 6 pipeline? (16)
- [ ] `grep -c "gemma4" ~/llm-gateway/pipelines.yaml` — Gemma 4 присутствует в pipeline? (10D)
- [ ] `ls ~/llm-gateway/scripts/` — auto-review.sh, auto-commit-msg.sh, auto-docs.sh, все +x?
- [ ] `ls ~/llm-gateway/.githooks/` — pre-push присутствует, +x?
- [ ] `git -C ~/llm-gateway config core.hooksPath` — `.githooks`?
- [ ] `sqlite3 ~/llm-gateway/ondo_demo.db "SELECT COUNT(*) FROM employees"` — 30? (11A)
- [ ] `~/llm-gateway/venv/bin/python ~/llm-gateway/scripts/text2sql_poc_v2.py --question "Скільки працівників?" --model qwen3-coder:30b` — SQL генерируется? (11A)
- [ ] Конфиг Continue `%USERPROFILE%\.continue\config.yaml`: **10 моделей** (gemma4-31b, gemma4-26b в Tier 1), 12 providers, 6 prompts, MCP git + rag подключены?
- [ ] Глобальные rules (01-general.md, 02-coding.md, **03-security.md**) актуальны? (13)
- [ ] `systemctl status rag-mcp.service` → active (running)? (11)
- [ ] `cat ~/rag-mcp-server/index_status.json | jq '.chunks_indexed'` → 12? (13, ADR-only primary)
- [ ] `sudo ufw status` → active? Порты 8000, 8100, 11434, 22 — LAN-only? (11)
- [ ] `bash ~/llm-gateway/scripts/health-check.sh` → 11/11 passed? (15)
- [ ] `bash ~/llm-gateway/scripts/test_stage16.sh` → 13/13 PASS? (16)
- [ ] `~/llm-gateway/venv/bin/python ~/llm-gateway/scripts/benchmark.py --output ~/llm-gateway/benchmarks/` → 7/7 passed? (15)
- [ ] Baseline в `~/llm-gateway/benchmarks/` актуален? Если обновлялся Ollama/модели — перезапустить benchmark. (15)
- [ ] `bash ~/llm-gateway/scripts/setup-check.sh` → PASS? (13)
- [ ] На клиенте: `powershell -ExecutionPolicy Bypass -File setup-check.ps1` → PASS? (13)

### 15.5. Smoke-тест headless автоматизации

```bash
cd ~/llm-gateway

# Тест orchestrator --stdout (standalone CLI)
./venv/bin/python orchestrator.py --pipeline smoke --task "Hello 8D" --stdout

# Тест gateway orchestrate (API)
source <(set -a && source ~/llm-gateway/.env && set +a && env)
TOKEN=$(echo "$LLM_GATEWAY_TOKENS" | python3 -c "import sys,json; print(list(json.load(sys.stdin).keys())[0])")
curl -sf -X POST http://localhost:8000/v1/orchestrate -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"pipeline":"smoke","task":"Hello 16"}' | jq '.steps[0].output[:100]'

# Тест auto-review (нет staged = пустой diff, должен выйти с exit 0)
./scripts/auto-review.sh --staged

# Тест auto-docs
./scripts/auto-docs.sh gateway.py --format markdown | head -20
```

### 15.6. Новые технологии, не отражённые в паспорте

- [ ] Появились ли новые модели в Ollama, не внесённые в таблицу 5.2?
- [ ] Подключены ли новые MCP-серверы, не отражённые в таблице 7.3?
- [ ] Появились ли новые файлы конфигурации в `~/llm-gateway/`, `~/rag-mcp-server/` или `%USERPROFILE%\.continue\`?
- [ ] RAG MCP server отражён в паспорте (секции 7.3, 8.4, 10, 12)?
- [ ] rag.yaml присутствует в `.continue/mcpServers/`?
- [ ] Все изменения зафиксированы в паспорте и changelog (раздел 16)?

---

## 16. История версий (Changelog)

| Версия | Дата | Изменения |
|--------|------|-----------|
| v1–v9 | 2026-03-15 — 2026-03-20 | Поэтапное построение стека: gateway.py v0.1.0 → v0.7.0, Continue.dev интеграция, Ollama 0.18.0, 4 новых модели, Copilot BYOK |
| v10.0 | 2026-03-21 | Этап 8A: mcp-server-git (STDIO, uvx, 12 tools), uv 0.10.12; PoC MCP-интеграции подтверждён |
| v11.0 | 2026-03-27 | Политика актуальности, регулярная ревизия, чек-лист, история версий. Статусы во всех ключевых таблицах. Roadmap gateway.py. Политика квантизации KV-кэша. |
| v12.0 | 2026-03-27 | Этап 8B: Terminal Policy Framework — Active. 01-general.md (alwaysApply). Terminal row в MCP table Planned → Active. Windows/Terminal грабли. Целевая архитектура v1.4. |
| v13.0 | 2026-03-27 | Этап 8C: Orchestrator PoC — Active. orchestrator.py + pipelines.yaml (5 pipeline). Planner=qwen3:30b. Время plan-execute-review: 53–88 сек. Раздел 8 (Orchestration). |
| **v14.0** | **2026-03-27** | **Этап 8D: Headless Automation PoC — Active. orchestrator.py → v1.1.0 (+--stdout, исправлен load_pipelines). pipelines.yaml +commit-msg (qwen3.5:9b, 1 шаг). scripts/auto-review.sh (3 режима, MAX_LINES=400). scripts/auto-commit-msg.sh. scripts/auto-docs.sh (docstring/markdown). .githooks/pre-push (STRICT_REVIEW + fail-closed non-TTY). git config core.hooksPath .githooks. Все 7 сценариев подтверждены. Чек-лист 14.4+14.5 обновлён. Грабли: venv/python-только, load_pipelines fix, -t 0 TTY check.** |
| **v15.0** | **2026-03-28** | **Этап 9A: gateway.py v0.7.0 → v0.8.0. Добавлен POST /v1/embeddings: OpenAI-compatible, batch support, staged pipeline validation, allowlist (qwen3-embedding). qwen3-embedding: Planned → Active (backend). Негативные тесты: empty_input (400), schema 422, unsupported_model (400). Задеплоен через systemd. Чек-лист 14.2+14.4 обновлён (новая проверка /v1/embeddings). Roadmap gateway обновлён. Документационный дрейф: gateway.py docstring частично устарел (будет исправлен в 10A при модуляризации). Модуляризация явно вынесена за рамки 9A → 10A.** |
| **v16.0** | **2026-03-29** | **Этап 9B: Continue embeddings migrated transformers.js → qwen3-embedding через gateway. Transport path подтверждён (batched 200 OK, automatic reindex). gateway.py hotfix: encoding_format silent coerce (вместо 400 reject). Continue.dev: 1.2.17 → 1.2.22 (documentation drift исправлен). provider: codebase добавлен (deprecated upstream, но функционален). Грабли: encoding_format, apiKey заглушка, Windows EBUSY, @Codebase deprecated. Чек-лист 14.1+14.4 обновлён. Roadmap 9A+9B → Complete.** |
| **v17.0** | **2026-03-31** | **Этап 10A: gateway v0.8.0 → v0.9.0. Монолит разбит на пакет gateway/ (8 модулей, ADR-008 закрыт). Structured JSON logging: Custom JSONFormatter (stdlib), события llm_completion (chat) + llm_embedding (embed), uvicorn.access заменён на свой middleware. run.py entrypoint. systemd ExecStart обновлён. Docstring MAX_LOADED_MODELS=2 исправлен. Грабли #30 (systemd placeholder), #31 (scp mkdir). Чек-лист 14.1+14.4 обновлён. Roadmap gateway v0.9.0 → Complete.** |
| **v18.0** | **2026-04-03** | **Этап 10B: gateway v0.9.0 → v0.10.0. Новый модуль metrics.py (9-й в пакете). GET /metrics: JSON in-memory counters (totals, endpoints, models). MetricsCollector singleton, thread-safe. collector.record() в 8 точках (5 chat + 3 embed). /metrics освобождён от Bearer auth (AUTH_EXEMPT_PATHS). Observability baseline закрыт: health + structured logs + metrics. Грабли #32 (deployment file mismatch). Чек-лист 14.1+14.4 обновлён. Roadmap gateway v0.10.0 → Complete.** |
| **v19.0** | **2026-04-03** | **Этап 10C: Ollama 0.18.0 → 0.20.0. +3 модели Gemma 4 (31b Active, 26b Active, e4b PoC): 13→16. Раскладка ролей радикально обновлена (ADR-012): gemma4:31b=Quality-first Agent/Planner/Reviewer (#3 Arena), gemma4:26b=Fast Semantic Agent/Executor (#6 Arena, MoE 3.8B active), qwen3-coder:30b понижен до Stable Generic Executor/Backup. deepseek-coder-v2:16b→Deprecated. qwen3-vl:8b, glm-4.7-flash, qwen3:30b→Reserve. qwen3.5:9b tool calling восстановлен. Целевая архитектура v1.6→v1.7. Грабли #33 (override.conf backup). Чек-лист обновлён (Ollama 0.20.0, 16 моделей).** |
| **v20.0** | **2026-04-04** | **Этап 10D: Config Sync. Continue config.yaml: 8→10 моделей (+gemma4-31b, gemma4-26b), Tier-организация по ADR-012. pipelines.yaml: 4 pipeline обновлены (execute-review=gemma4:26b→31b, plan-execute-review=31b→26b→31b, review-only=31b, docs-generate=26b). End-to-end verified: Chat (обе Gemma), Agent mode MCP tool use (обе Gemma, grounded), orchestrator execute-review (Fibonacci), /metrics delta (+2/+2). Headless scripts косвенно обновлены через pipeline. Секции 7.2, 7.4, 7.5, 8.1, 10, 11, 14.4 обновлены. Грабли #34 (datetime.utcnow DeprecationWarning в orchestrator.py).** |
| **v21.0** | **2026-04-04** | **Этап 11A: Text-to-SQL PoC (Track F: AI-as-Interface). NL→SQL viability подтверждена на SQLite (6 таблиц, 171 запись, ONDO-тематика). 20 тестовых вопросов, 3 модели. Baseline truth (v2 evaluator): qwen3-coder:30b SA 75%, gemma4:31b SA 70%, gemma4:26b SA 65%. Best observed (v3 + prompt addendum): SA 85–90% (все модели, +15–20 п.п., model-agnostic). qwen3-coder:30b — лучший SQL executor (0.4s latency). Safety/hallucination: 100% pass rate. Prompt bleed зафиксирован при micro-tuning (Грабли #36). ADR-013: evaluator separation + prompt freeze. qwen3-coder:30b → +Best SQL Executor role. Новая секция 8.3. Новые файлы в секции 11 (9 артефактов). Чек-лист 14.4 обновлён. Грабли #35–37.** |
| **v22.0** | **2026-04-05** | **Этап 11: MCP RAG / Docs Search. Custom RAG MCP server (FastMCP, mcp 1.27.0, streamable-http, порт 8100). ChromaDB 1.5.5 PersistentClient: 133 файла / 286→297 chunks, collection lab_docs. Embedding: qwen3-embedding (4096d) через gateway /v1/embeddings. 5 tools: search_docs, get_doc_section, grep_doc, list_collections, index_status. Continue Agent mode: rag.yaml (streamable-http), 9/10 релевантных результатов, кириллица работает, cross-tool (git+RAG) подтверждён. systemd rag-mcp.service (After=llm-gateway). UFW 8100/tcp LAN-only. UFW was inactive — enabled (Грабли #42). ADR-014: streamable-http для серверных MCP. Runtime-верификация моделей: таблица 5.2 обновлена по `ollama show` (31.3B, 25.8B, qwen3-coder=MoE 30.5B, qwen3-embedding=7.6B/4096d/Q4_K_M). OLLAMA_NUM_PARALLEL=1 добавлен в таблицу 5.1 (устранена несогласованность с чек-листом). Проверка dim=4096 добавлена в чек-лист 14.4. Секции 3, 5.1, 5.2, 6.1, 7.1–7.4, 8.4 (новая), 9, 10, 11, 14.4, 14.6, 15 обновлены. Грабли #38–42.** |
| **v23.0** | **2026-04-05** | **Этап 15: Benchmark Matrix + Health Automation. health-check.sh (11/11), benchmark.py (7/7), baseline snapshot. 128K context window: Blocked/External — upstream bug Ollama 0.20.0 / llama.cpp SWA (Грабли #43, Gemma 4 стабильна при ctx ≤ 32768, stall при 65K+ — hardware-agnostic). Gateway fixes: MAX_NUM_CTX 32768→131072, HTTPX_TIMEOUT 600s→3600s (Грабли #44–45). Long-context limitation добавлена в таблицу 5.2. Новая секция 8.5 (Health + Benchmark). Чек-лист 14.4 обновлён (+health-check, +benchmark). Секции 5.2, 6.2, 8.5 (новая), 10, 11, 14.4, 15 обновлены. Грабли #43–45.** |
| **v24.0** | **2026-04-07** | **Этап 14 (A+B): Security Hardening завершён. 14A: UFW least-privilege (default deny, 8 правил, dual-zone LAN+WG), nightly health-check cron, benchmark history wrapper (immutable snapshots), Ollama Canary SOP, Ollama 0.20.0→0.20.2. 14B: gateway v0.10.0→v0.11.0, mandatory per-user Bearer auth (2 users: vladimir+roadwarrior), user_id audit trail в structured logs, auth.py (10-й модуль пакета), .env token storage (chmod 600, shared EnvironmentFile). RAG MCP regression (#48): gateway_embeddings.py патч (Bearer auth), rag-mcp.service +EnvironmentFile. RAG MCP debug prints удалены. deepseek-coder-v2:16b удалена (15 моделей). Accumulated drift v23 закрыт.** |
| **v25.0** | **2026-04-07** | **Этап 16: gateway v0.11.0→v0.12.0. Новый модуль orchestrate.py (11-й). POST /v1/orchestrate (sequential multi-model pipeline, self-call pattern, OpenAI SDK + service token `orchestrator`). GET /v1/orchestrate/pipelines (pipeline registry API). 3-й user `orchestrator` в .env. /health: +pipelines_count. /metrics: /v1/orchestrate tracked. Structured log: llm_orchestrate. Gemma 4 pipeline подтверждён. 7 integration defects исправлены. 13/13 tests PASS. Грабли #49 (shell .env JSON quoting), #50 (module integration contract), #51 (jq multiline tail). ADR-016 (sync orchestrate + self-call).** |
| **v26.0** | **2026-04-08** | **Этап 13: Knowledge Layer завершён. 03-security.md → Active (alwaysApply, denylist, git safety, токены). ADR bridge: 7 canonical + 5 foundational ADR в RAG (12 files/12 chunks, ADR-only primary corpus). ADR multilayer model: foundational/canonical в primary, subsidiary/operational/closed в backup-зонах. Obsidian Решения/ нормализованы (5 каталогов). ONBOARDING.md (disaster recovery навигатор). setup-check.sh (52 проверки сервера), setup-check.ps1 (16 проверок клиента). Ubuntu 22.04→24.04 (doc drift #56). Ollama override восстановлен: +NUM_PARALLEL=1 +OLLAMA_HOST (#57). git.yml→git.yaml (#58). Целевая архитектура v1.11→v1.12. Грабли #52–58.** |
